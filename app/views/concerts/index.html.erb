<h1>Concerts</h1>

<% @concerts.each do |concert| %>
    <h4><%= link_to "#{concert.artist.name} at #{concert.venue.name}", concert_path(concert) %></h4>
    <h2><%= concert.display_date %></h2>
<% end %>
    